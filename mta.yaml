_schema-version: "3.2"
ID: postmaterialdocument
version: 0.0.1
modules:
- name: postmaterialdocument-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: postmaterialdocument-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - materialdocument.zip
      name: materialdocument
      target-path: resources/
- name: materialdocument
  type: html5
  path: materialdocument
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: postmaterialdocument-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: postmaterialdocument-html5-service
    service-plan: app-host
- name: postmaterialdocument-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: postmaterialdocument-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
